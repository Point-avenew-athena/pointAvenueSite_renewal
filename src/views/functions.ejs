<%
//* 시간 포맷 변환
const dateByTimezone = (
  time,
  format = process.env.DEFAULT_DATEFORMAT,
  timezone = process.env.DEFAULT_TIMEZONE,
) => {
  const timeFormat1 = moment.tz(time, timezone);
  const convertedDate = timeFormat1.format(format);
  return convertedDate;
};
%>