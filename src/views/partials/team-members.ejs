<div class="swipe_type_01_wrap">
  <div class="swiper-container">
    <ul class="swiper-wrapper swipe_type_01 team_list">
      <% for ( let i = 0; i < teamMembers.length; i++) { %>
        <li class="swiper-slide swipe_type_01--item team_item">
          <a href="javascript:void(0);" 
             data-id="<%= teamMembers[i].id %>" 
             data-name="<%= teamMembers[i].name %>" 
             data-position="<%= teamMembers[i].position %>" 
             data-img="<%= teamMembers[i].img %>" 
          >
            <div class="swipe_type_01--img team_img">
              <div class="img_box">
                <img src="<%= teamMembers[i].img %>" alt="<%= teamMembers[i].name %>">
              </div>
            </div>
            <div class="swipe_type_01--desc team_desc_box">
              <p class="title_r_03"><%= teamMembers[i].name %></p>
              <div class="title_r_03--gray"><%= teamMembers[i].position %></div>
            </div>
          </a>
        </li>
      <% } %>
    </ul>

    <!-- If we need navigation buttons -->
    <div class="swiper-button-prev"></div>
    <div class="swiper-button-next"></div>
  </div>
</div>